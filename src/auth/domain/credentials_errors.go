package domain

const (
	ErrOwnerIDRequired  = "credentials.owner-id.required"
	ErrEmailRequired    = "credentials.email.required"
	ErrEmailInvalid     = "credentials.email.invalid"
	ErrPasswordRequired = "credentials.password.required"
	ErrPasswordTooShort = "credentials.password.too-short"
	ErrPasswordTooLong  = "credentials.password.too-long"
)
